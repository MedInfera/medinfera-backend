services:
  - type: web
    name: medinfera-backend
    runtime: node
    branch: main
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production